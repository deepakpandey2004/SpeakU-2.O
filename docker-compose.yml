version: '3.8'

services:
  app:
    build: .
    container_name: speaku_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
      - ./frontend:/app/frontend
    restart: unless-stopped
    command: uvicorn run:app --host 0.0.0.0 --port 8000 --reload